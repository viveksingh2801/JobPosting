// import { createSlice } from "@reduxjs/toolkit";

// const userJobsSlice = createSlice({
//   name: "userJobs",
//   initialState: [],
//   reducers: {
//     addUserJob: (state, action) => {
//       state.push(action.payload);
//     },
//     removeUserJob: (state, action) => {
//       return state.filter((job) => job._id !== action.payload);
//     },
//     setUserJobs: (state, action) => {
//       return action.payload; // Agar tum ek sath sare user jobs set karna chaaho
//     }
//   },
// });

// export const { addUserJob, removeUserJob, setUserJobs } = userJobsSlice.actions;
// export default userJobsSlice.reducer;
